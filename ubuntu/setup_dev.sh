#!/usr/bin/env sh


#---------- Programming, build tools and libraries ----------

# build essentials
sudo apt-get install build-essential gcc gcc-doc g++ glibc-doc
# libstdc++6 and dev version
# libstdc++5
# debug version also
# Install documentation also

# Editors
sudo apt-get install vim vim-gtk vim-common vim-doc

# cmake
sudo apt-get install pkg-config
sudo apt-get install cmake cmake-gui

# Version control
sudo apt-get install git
sudo apt-get install subversion subversion-tools

# Boost libraries
sudo apt-get install libboost-all-dev

# Parser stuff
# sudo apt-get install xml-core

# Documentation generators
sudo apt-get install doxygen
